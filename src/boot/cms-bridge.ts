import { boot } from 'quasar/wrappers';

// We leave the arguments empty to satisfy the 'no-unused-vars' rule 
// until we are ready to register global WaterMap/HealthCard components.
export default boot(() => {
  console.log("PON.INK: CMS Bridge Initialized - Signal Clear.");
});
